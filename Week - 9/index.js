// Types or interfaces are used to manage objects with multiple variables

// type has an assignment operator, interface doesnt

// Optional parameters in interfaces/types have question marks

// react hooks - useState, useEffect, useRef, 

// follow hook conventions e.g
// const [addCart, SetAddCart] = useState(0)

// errors occuring within a route (both dynamic and static), you MUST use error.tsx
// errors dealing with routes directly, you MUST use not-found.tsx

// COMPONENTS THEMSELVES ARE ONE COMMIT